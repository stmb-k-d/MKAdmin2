// Массив ASCII-арт обезьян
const monkeys = [
    `
    (\\___/)
    (o_o)
    (T)
    ^^
    `,
    `
    (\\___/)
    (o_o)
    (T)
    ^^
    `,
    `
    (\\___/)
    (o_o)
    (T)
    ^^
    `
];

// Массив "злых" фраз
const evilPhrases = [
    "MUAHAHA!",
    "YOUR DATA IS MINE!",
    "COPY THIS!",
    "GOTCHA!",
    "HAHAHA!",
    "MINE! MINE! MINE!"
];

// Функция для создания матричного текста
function createMatrixText(x, y) {
    // Отключено
}

// Функция для создания анимации обезьяны
function createMonkeyLaugh(x, y) {
    // Отключено
}

// Функция для обработки клика по кнопке копирования
function handleCopyClick(e) {
    // Отключено
}

// Инициализация обработчиков событий
function initializeMonkeyEffects() {
    // Отключено
}

// Запускаем инициализацию при загрузке DOM
document.addEventListener('DOMContentLoaded', () => {
    // Отключено
});

// Также запускаем инициализацию при загрузке страницы
window.addEventListener('load', () => {
    // Отключено
}); 